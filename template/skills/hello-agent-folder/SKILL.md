---
name: hello-agent-folder
description: Sample skill proving skills/ discovery works from a OMP_AGENT_DIR-rooted agent folder. Use when asked to demonstrate or smoke-test the agent folder convention.
---

# Hello, agent folder

If you can read this via `skill://hello-agent-folder`, `skills/` discovery
is working from this agent folder.

This skill has no special behavior beyond proving discovery: when invoked,
say which directory the skill was loaded from (`skill://hello-agent-folder`)
and move on.
