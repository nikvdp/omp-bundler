---
description: Report agent-folder discovery status
---

Confirm the following and report the result in one short list:

- `skill://hello-agent-folder` reads without error.
- The `workspace_info` tool is present in the active tool set.
- The `reviewer-lite` agent is listed as spawnable by the task tool.
- This command (`commands/status.md`) itself expanded correctly.
